export default class WordFocuses {
    constructor( count, first, last, duration ) {
        this.count = count;         // int
        this.first = first;         // ms
        this.last = last;           // ms
        this.duration = duration;   // ms
    }
}
