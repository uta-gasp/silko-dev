export default class Rect {
    constructor( x, y, width, height ) {
        this.x = x;             // px
        this.y = y;             // px
        this.width = width;     // px
        this.height = height;   // px
    }
}
