{
  "compilerOptions": {
    "alwaysStrict": true,
    "checkJs": true,
    "target": "ES6",
    "baseUrl": "./src/",  // all paths are relative to the baseUrl
    "paths": {
        "@/*" : ["*"],   // resolve any `@/foo/bar` to `<baseUrl>/foo/bar`
        "~/*" : ["*"]    // resolve any `~/foo/bar` to `<baseUrl>/foo/bar`
      }
  }
}
